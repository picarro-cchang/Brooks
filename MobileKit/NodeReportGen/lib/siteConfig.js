/* siteConfig.js defines the object that stores site configuration data for the node report generation program. */
/*global exports, module, require */
if (typeof define !== 'function') { var define = require('amdefine')(module); }

define(function(require, exports, module) {
	'use strict';
    module.exports = {
    };
});
