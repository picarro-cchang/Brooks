export XKL_XMODMAP_DISABLE=1
#export DISPLAY=:1
#autocutsel -fork
#export PATH=/opt/conda/bin:$PATH
# Run the profile to set paths etc. Note that "source" does not work
. /etc/profile
exec /usr/bin/startlxde
