influxdb:
  database: "pigss_data"
  measurements: ["crds", "port_history"]
  host: 0.0.0.0
  port: 8086
  timeout: 300
  retries: 1

# Config for server
server:
  host: 0.0.0.0
  port: 8010
  data_dir: "/tmp"
  file_type: "csv"

user_keys: ["H20", "CO2", "NH3", "CH4", "HF", "HCl"]
