Run buildCWSInstaller.py to make installer. 

The CWS installer adds a priming program to HostExe folder, and also adds Coordinator_CWS.ini to AppConfig\Config\Coordinator.

Host software must be installed before running CWS installer.