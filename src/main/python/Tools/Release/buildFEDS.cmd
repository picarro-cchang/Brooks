echo "Ensure sandbox and host are up to date (git status;git commit; git pull"
echo "Control-C to abort; any other key to proceed"
release.py --product=mobile --branch=surveyor-2.0.0 --debug-skip-all-clone 